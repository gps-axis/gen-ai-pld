# Kept in step with pyproject.toml by hand: the image builds from this file
# and the host venv from uv.lock, and the two must agree or the container runs
# code that was never exercised on the host.
fal-client==1.0.1
numpy==2.5.2
pillow==12.3.0
pillow-heif==1.6.0
requests==2.34.2
scipy==1.18.1
